Issues:
  ☐ does not work when the redhat yaml extension is installed.

Code:
  ☐ open traefik docs
  ☐ only work with traefik.*.ya?ml
    ☐ provide intellisense inside nested traefik config yaml
      ☐ this works with json schema
  ☐ Work with Dockerfile

Snippets:

Docker:
  Config:
    ✔ config file static/dynamic json schema
    ☐ config file yaml
      ☐ exposedbydefault https://docs.traefik.io/providers/docker/#exposedbydefault
    Http:
    Tcp:

Todo:
  ☐ check if inside the lables tag

Archive:
  ✔ envars snippets @project(Snippets.Env vars)
  ✔ https://docs.traefik.io/reference/static-configuration/cli/ @project(Traefik.Cli)
  ✔ accesslog @project(Traefik.Cli)
  ✔ api @project(Traefik.Cli)
  ✔ certificatesresolvers @project(Traefik.Cli)
  ✔ entrypoints @project(Traefik.Cli)
  ✔ global @project(Traefik.Cli)
  ✔ hostresolver @project(Traefik.Cli)
  ✔ log @project(Traefik.Cli)
  ✔ metrics @project(Traefik.Cli)
  ✔ ping @project(Traefik.Cli)
  ✔ providers (rest) @project(Traefik.Cli)
  ✔ serverstransport @project(Traefik.Cli)
  ✔ tracing @project(Traefik.Cli)
  ✔ only work with docker-compose.*.ya?ml @project(Code)
  ✔ entrypoints @project(Traefik.Cli)
  ✔ trigger snippets if prefix is '- ' @project(Todo)
  ✔ stop suggestion after =  @project(Code)
  ✔ traefik api @project(Snippets)
  ✔ intellisense for docker providers @project(Traefik.Providers)
  ✔ kubernetes ingress route @project(Traefik.Providers)
  ✔ kubernetes ingress @project(Traefik.Providers)
  ✔ consul @project(Traefik.Providers)
  ✔ marathon @project(Traefik.Providers)
  ✔ rancher @project(Traefik.Providers)
  ✔ file @project(Traefik.Providers)
  ✔ api config options @project(Traefik.Api)
  ✔ middlewares @project(Docker.Http)
  ✔ docker @project(Docker.Http)
  ✔ routers @project(Docker.Http)
  ✔ services @project(Docker.Http)
  ✔ routers @project(Docker.Tcp)
  ✔ services @project(Docker.Tcp)
